<%- include('../partials/header') %>

<div class="page-head">
  <div>
    <p class="section-label">Posiciones</p>
    <h1>Tabla de posiciones</h1>
  </div>
</div>

<div class="notice">
  Acertar el ganador suma <strong><%= settings.pointsOutcome %> puntos</strong>;
  acertar el marcador exacto suma <strong><%= settings.pointsExact %> puntos</strong>.
</div>

<% if (settings.predictionsLocked) { %>
  <div class="notice">
    La carga está cerrada: toca el nombre de cualquier socio para ver su
    quiniela. Las quinielas son visibles para todos por transparencia.
  </div>
<% } %>

<%- include('../partials/standings') %>

<%- include('../partials/footer') %>
